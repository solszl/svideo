import Plugin from '../core/Plugin';

/**
 * 截图接口
 *
 * @export
 * @class Capture
 * @extends {Plugin}
 * @author zhenliang.sun
 */
export default class Capture extends Plugin {
  constructor() {
    super();
  }
}