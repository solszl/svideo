import PlayerProxy from './../PlayerProxy'

/**
 * 抽象播放器
 *
 * @export
 * @class AbstractPlayer
 * @extends {Component}
 * @author zhenliang.sun
 */
export default class AbstractPlayer extends PlayerProxy {
  constructor() {
    super()
  }
}
