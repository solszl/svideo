import EventEmitter from 'event-emitter';

/**
 * FLV 数据解析器
 *
 * @export
 * @class FlvParser
 * @author zhenliang.sun
 */
export default class FlvParser {
  constructor() {
    EventEmitter(this);
  }
}